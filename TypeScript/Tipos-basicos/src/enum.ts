// enum

/*O tipo enum é usado para representar um conjunto de valores nomeados. 
Por exemplo, podemos criar um enum para representar usuários de um sistema: */

enum UserRoles {
    USER = "user",
    EDITOR = "editor",
    ADMIN = "admin"
};

const userTyper = UserRoles.USER;